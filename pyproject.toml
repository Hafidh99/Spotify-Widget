[project]
name = "spotify-widget"
version = "0.1.0"
description = "Spotify Now Playing Widget for GitHub Profile"
dependencies = [
    "flask",
    "requests",
    "python-dotenv",
    "colorthief",
    "Pillow"
]

[tool.vercel]
entrypoint = "api.orchestrator:app"